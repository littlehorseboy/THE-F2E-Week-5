const store = new Vuex.Store({
  state: {
    
  },
  getters: {
    // getProductById: (state) => (id) => {
    //   return state.products.find(product => product.id === id);
    // },
  },
  actions: {
    
  },
  mutations: {
    
  },
});
